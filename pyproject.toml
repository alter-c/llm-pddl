[project]
name = "llm-pddl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backoff",
    "openai",
    "pytest",
    "pddl",
    "langchain-core",
    "langchain_openai",
    "langgraph",
    "nvitop",
]
